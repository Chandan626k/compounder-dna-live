name: Phase 2 Market Structure Plan

on:
  push:
    branches:
      - remediation/verified-data-contract

jobs:
  plan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate planner syntax
        run: node --check scripts/phase2-market-structure-plan.mjs
      - name: Generate candidate
        run: node scripts/phase2-market-structure-plan.mjs
      - name: Generate equivalent diff
        run: git diff --no-index -- lib/canonical-technical-engine.js /tmp/canonical-technical-engine.structure-candidate.js > /tmp/canonical-market-structure.diff || test $? -eq 1
      - name: Validate candidate syntax
        run: node --check /tmp/canonical-technical-engine.structure-candidate.js
      - name: Upload candidate diff
        uses: actions/upload-artifact@v4
        with:
          name: phase2-canonical-market-structure-diff
          path: /tmp/canonical-market-structure.diff
          if-no-files-found: error
