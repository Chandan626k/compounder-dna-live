import { verifiedHistory, verifiedQuote } from '../lib/market-data-provider.js';
import YahooFinance from 'yahoo-finance2';

const yf = new YahooFinance();
const CORS = { 'Access-Control-Allow-Origin': process.env.ALLOWED_ORIGIN || '*', 'Access-Control-Allow-Methods': 'GET, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type', 'Cache-Control': 'no-store' };
const num = (v) => typeof v === 'number' && Number.isFinite(v) ? v : (v && Number.isFinite(v.raw) ? v.raw : null);
const ticker = (s) => { const x=String(s||'').trim().toUpperCase().replace(/\s+/g,''); if(!x) throw Error('Stock symbol is required'); return x.endsWith('.NS')||x.endsWith('.BO')?x:`${x}.NS`; };
const iso = (v) => { const d=v instanceof Date?v:new Date(v); return Number.isNaN(d.getTime())?null:d.toISOString(); };
const normalize = (data, expectedPeriod, fields) => (Array.isArray(data)?data:[]).map(r=>{const date=iso(r?.date), periodType=r?.periodType==null?null:String(r.periodType).toUpperCase(); if(!date || periodType !== expectedPeriod) return null; const out={date,period:expectedPeriod,periodType}; for(const f of fields) out[f]=num(r?.[f]); return out;}).filter(Boolean).sort((a,b)=>new Date(a.date)-new Date(b.date));
const coverage = (records, fields) => { const available=fields.filter(f=>records.some(r=>r[f]!=null)).length; return {available,total:fields.length,percentage:fields.length?Math.round(available/fields.length*100):0}; };

async function foundation(symbol) {
  const now=new Date(), p5=new Date(Date.now()-5*365.25*86400000);
  const annualFields=['totalRevenue','operatingIncome','netIncomeFromContinuingAndDiscontinuedOperation','dilutedEPS','freeCashFlow','operatingCashFlow','capitalExpenditure','totalDebt','cashCashEquivalentsAndShortTermInvestments','stockholdersEquity','totalAssets'];
  const quarterlyFields=['totalRevenue','operatingIncome','netIncomeFromContinuingAndDiscontinuedOperation','dilutedEPS','freeCashFlow','operatingCashFlow','capitalExpenditure'];
  const [quote,annual,quarterly,historyResult,currentQuoteResult]=await Promise.allSettled([
    yf.quoteSummary(symbol,{modules:['price','quoteType','summaryDetail','defaultKeyStatistics','financialData','assetProfile','majorHoldersBreakdown','institutionOwnership','insiderHolders','insiderTransactions','earningsTrend','calendarEvents']},{validateResult:false}),
    yf.fundamentalsTimeSeries(symbol,{period1:p5,period2:now,type:'annual',module:'all'},{validateResult:false}),
    yf.fundamentalsTimeSeries(symbol,{period1:p5,period2:now,type:'quarterly',module:'all'},{validateResult:false}),
    verifiedHistory(symbol,{interval:'1d',days:3650,minBars:60}),
    verifiedQuote(symbol),
  ]);
  const q=quote.status==='fulfilled'?quote.value:null;
  const ar=normalize(annual.status==='fulfilled'?annual.value:[],'12M',annualFields);
  const qr=normalize(quarterly.status==='fulfilled'?quarterly.value:[],'3M',quarterlyFields);
  const history=historyResult.status==='fulfilled'?historyResult.value:null;
  const currentQuote=currentQuoteResult.status==='fulfilled'?currentQuoteResult.value:null;
  const pr=(history?.rows||[]).map(r=>({date:iso(r.date),open:num(r.o),high:num(r.h),low:num(r.l),close:num(r.c),volume:num(r.v)})).filter(r=>r.date&&r.close!=null);
  const sd=q?.summaryDetail||{}, ks=q?.defaultKeyStatistics||{}, fd=q?.financialData||{};
  const market={price:num(currentQuote?.price)??pr.at(-1)?.close??null,currency:currentQuote?.currency||q?.price?.currency||null,exchange:currentQuote?.exchange||q?.price?.exchangeName||null,marketCap:num(sd.marketCap)??num(ks.marketCap)??null,high52:num(sd.fiftyTwoWeekHigh),low52:num(sd.fiftyTwoWeekLow),beta:num(ks.beta),averageVolume:num(sd.averageVolume),trailingPE:num(sd.trailingPE),forwardPE:num(sd.forwardPE),priceToBook:num(ks.priceToBook),enterpriseValue:num(ks.enterpriseValue),sharesOutstanding:num(ks.sharesOutstanding),roe:num(fd.returnOnEquity)!=null?num(fd.returnOnEquity)*100:null,roa:num(fd.returnOnAssets)!=null?num(fd.returnOnAssets)*100:null,debtToEquity:num(fd.debtToEquity)!=null?num(fd.debtToEquity)/100:null,revenueGrowth:num(fd.revenueGrowth)!=null?num(fd.revenueGrowth)*100:null,earningsGrowth:num(fd.earningsGrowth)!=null?num(fd.earningsGrowth)*100:null};
  const warnings=[]; if(!q)warnings.push('Quote/fundamental summary unavailable.'); if(!ar.length)warnings.push('Annual 12M history unavailable.'); if(!qr.length)warnings.push('Quarterly 3M history unavailable.'); if(!pr.length)warnings.push('Verified daily price history unavailable.'); else if(pr.length<252)warnings.push(`Only ${pr.length} daily rows returned; 1Y history is incomplete.`); if(annual.status==='rejected')warnings.push(`Annual provider error: ${annual.reason?.message||annual.reason}`); if(quarterly.status==='rejected')warnings.push(`Quarterly provider error: ${quarterly.reason?.message||quarterly.reason}`); if(historyResult.status==='rejected')warnings.push(`Verified history provider error: ${historyResult.reason?.message||historyResult.reason}`); if(currentQuote?.freshnessStatus==='STALE_DURING_REGULAR_SESSION')warnings.push('Current provider quote is stale during the regular session; latest verified daily bar is the safe price observation.');
  return {success:true,symbol:symbol.replace(/\.NS$|\.BO$/i,''),yahooSymbol:symbol,generatedAt:new Date().toISOString(),dataPolicy:{fakeDataAllowed:false,zeroFabrication:true,providerEstimatesAllowed:true,missingValueRepresentation:null,rule:'Only provider-reported values or deterministic calculations from provider-reported inputs are returned. Annual rows require explicit periodType=12M; quarterly rows require explicit periodType=3M; missing values remain null.'},market,profile:{name:q?.quoteType?.longName||q?.quoteType?.shortName||symbol,sector:q?.assetProfile?.sector||null,industry:q?.assetProfile?.industry||null,country:q?.assetProfile?.country||null},ownership:{insidersPct:num(ks.heldPercentInsiders)!=null?num(ks.heldPercentInsiders)*100:null,institutionsPct:num(ks.heldPercentInstitutions)!=null?num(ks.heldPercentInstitutions)*100:null,majorHolders:q?.majorHoldersBreakdown||null,institutionOwnership:q?.institutionOwnership?.ownershipList||[],insiderHolders:q?.insiderHolders?.holders||[],insiderTransactions:q?.insiderTransactions?.transactions||[]},history:{priceDaily:pr,annual:ar,quarterly:qr,coverage:{annual:coverage(ar,annualFields),quarterly:coverage(qr,quarterlyFields),dailyPriceRows:pr.length,dailyPriceYears:Number((pr.length/252).toFixed(1)),dailyHistorySource:history?.source||null,dailyHistoryVerified:Boolean(history?.verified)}},provenance:{provider:'Yahoo Finance via verifiedHistory + fundamentalsTimeSeries',marketAsOf:pr.at(-1)?.date||null,currentQuote:currentQuote?{price:currentQuote.price,observedAt:currentQuote.observedAt,retrievedAt:currentQuote.retrievedAt,marketState:currentQuote.marketState,exchangeDataDelayedBy:currentQuote.exchangeDataDelayedBy,quoteSourceName:currentQuote.quoteSourceName,freshnessStatus:currentQuote.freshnessStatus}:null,fetchedAt:new Date().toISOString(),annualPeriod:'explicit 12M only',quarterlyPeriod:'explicit 3M only',pricePeriod:'10Y requested',status:history?.verified?'PROVIDER_DATA_ONLY':'UNAVAILABLE'},warnings};
}
export default async function handler(req,res){Object.entries(CORS).forEach(([k,v])=>res.setHeader(k,v));if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return res.status(405).json({success:false,error:'Method not allowed'});try{return res.status(200).json(await foundation(ticker(req.query?.symbol)));}catch(error){console.error('[DATA FOUNDATION]',error?.message);return res.status(502).json({success:false,error:'Verified market data is temporarily unavailable. No fallback or fabricated values are used.'});}}
